<% if !Erp::Core.available?("online_store") %>
<li class="nav-item <%= "active open" if ["erp/stock_transfers/backend/transfers"].include?(params[:controller]) %>">
    <a href="javascript:;" class="nav-link nav-toggle">
        <i class="glyphicon glyphicon-transfer"></i>
        <span class="title"><%= t('.stock_transfer') %></span>
        <% if ["erp/stock_transfers/backend/transfers"].include?(params[:controller]) %>
            <span class="selected"></span>
            <span class="arrow open"></span>
        <% else %>
            <span class="arrow"></span>
        <% end %>
    </a>
    <ul class="sub-menu">
        <li class="nav-item">
            <a href="<%= erp_stock_transfers.backend_transfers_path %>" class="nav-link">
                <span class="title"><%= t('.transfers') %></span>
            </a>
        </li>
    </ul>
</li>
<% end %>